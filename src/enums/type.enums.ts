export enum Devices {
  MOBILE = 'mobile',
  DESKTOP = 'desktop',
}

export enum Collections {
  HD = '_hd',
  ALL = 'all',
  SLOTS = 'slots',
}

export enum EnumTypes {
  HOME = 'home',
  LOGIN = 'login',
  REGISTER = 'register',
}

